'use strict'

const store = require('../store')

const exampleGameBoard = ["","","","","","","","",""]

const isGameOver = gameBoard => {
  // Given a game board represented as an array of length 9 with elements
  // 'x', 'o', or '' (empty square), return 'x' if 'x' is the winner, 'o'
  // if 'o' is the winner, 'draw' if the game is over but has no winner,
  // and false if the game is not over
}

module.exports = {
  isGameOver
}
